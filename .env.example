# MySQL connection — update with your own credentials
DATABASE_URL="mysql://root:@localhost:3306/portfolio"

# Admin panel credentials
ADMIN_EMAIL=""
ADMIN_PASSWORD=""

# Secret used to sign the admin session JWT (change in production)
AUTH_SECRET="change-me-to-a-long-random-string"
